🎯 Детальное техническое задание:
Страница 1: Поиск (/search)

Поле ввода и кнопка "Найти".

При вводе названия (с дебаунсом!) отправляется запрос к API.

Отображается список карточек с результатами (постер, название, год).

На каждой карточке — кнопка "Добавить в избранное" (иконка сердца/звезды).

Если фильм уже в избранном — кнопка "активная".

При клике на карточку — переход на страницу фильма.

Страница 2: Детали фильма (/movie/:id)
Отображает подробную информацию: постер, название, год, рейтинг, сюжет, актеры и т.д.

Кнопка для добавления/удаления из избранного.

Кнопка "Назад к поиску".

Страница 3: Избранное (/favorites)
Список избранных фильмов (карточки, как на странице поиска).

Возможность удалить фильм из избранного (иконка "крестик" или "сердце").

При клике на карточку — переход на страницу фильма.

Если список пуст — показывать соответствующее сообщение.

Общие компоненты:
AppHeader — навигация между страницами.

MovieCard — карточка фильма (переиспользуется на странице поиска и в избранном).

SearchInput — умное поле ввода с дебаунсом.

Loader / Notification — компоненты для состояний загрузки и ошибок.

💡 Идеи для усложнения (опционально):
Добавить фильтры к поиску (год, тип: фильм/сериал).

Реализовать бесконечную прокрутку вместо пагинации.

Добавить теги к избранным фильмам (например, "комедия", "для вечера").

Использовать Teleport для модальных окон или уведомлений.

Сделать светлую/темную тему (через CSS-переменные и реактивное состояние).

🎁 Бонус: API для использования
OMDb API (простой, не требует ключа для демо, но лучше получить свой):

Базовый URL: https://www.omdbapi.com/

Пример запроса: https://www.omdbapi.com/?s=matrix&apikey=ваш_ключ

Детали фильма: https://www.omdbapi.com/?i=tt0133093&apikey=ваш_ключ

The Movie Database (TMDB) API (более современный, но требует регистрации для ключа):